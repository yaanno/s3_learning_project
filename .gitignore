# Generated by Cargo
# will have compiled files and executables
/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
# Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# IDE specific files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment variables
.env

# Build output for examples
target/

# For binary crates, you might want to commit this file.
# For library crates, you typically don't want to commit this file.
# Cargo.lock

# These are all the default files that Cargo generates.
# You probably don't want to commit them.
Cargo.lock

# These are all the files that `cargo build` generates.
# You probably don't want to commit them.
target/

# These are all the files that `cargo test` generates.
# You probably don't want to commit them.
target/

# These are all the files that `cargo doc` generates.
# You probably don't want to commit them.
/doc/

# These are all the files that `cargo run` generates.
# You probably don't want to commit them.
target/

# These are all the files that `cargo check` generates.
# You probably don't want to commit them.
target/
